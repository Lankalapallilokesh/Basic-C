#include<stdio.h>
int main()
{
	int n,a[10],i;
	printf("enter any number");
	scanf("%d",&n);
    printf("enter %d elements",n);
    for(i=0;i<n;i++)
    scanf("%d",&a[i]);
    printf("array elements are:");
    for(i=0;i<n;i++)
    printf("%d\n",a[i]);
}
