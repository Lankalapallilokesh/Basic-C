#include<stdio.h>
int main()
{
	int a[20],n,i,key,j;
	printf("enter no of elements");
	scanf("%d",&n);
	printf("enter %d element",n);
	for(i=0;i<n;i++)
	scanf("%d",&a[i]);
	printf("enter delete element");
	scanf("%d",&key);
	for(i=0;i<n;i++)
	{
		if(a[i]=key)
		{
			for(j=0;j<n;j++)
			a[j]=a[j+1];
			n--;
			
		}
		printf("\nafter delete element are:");
		for(i=0;i<n;i++)
		printf("%d",a[i]);
		
	}
	
	
}
